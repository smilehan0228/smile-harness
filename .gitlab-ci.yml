stages:
  - test
  - build

unit-test:
  stage: test
  image: python:3.11
  script:
    - pip install -e .
    - pip install pytest
    - make test

docker-build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build .
